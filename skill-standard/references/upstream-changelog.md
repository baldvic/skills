# Upstream changelog

Append-only log of syncs from [upstream-sources.md](upstream-sources.md). Maintained by the pipeline in [upstream-sync.md](upstream-sync.md).

## (initial)

- skill-standard created from agentskills format docs + anthropics skill-creator eval/benchmark artifacts; harness-agnostic editorial layer applied.
- `upstream.lock.json` added; `resolved_commit` null until first automated sync.
